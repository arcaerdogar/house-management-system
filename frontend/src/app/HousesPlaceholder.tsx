/** Shell placeholder — replaced by frontend-houses agent. */
export function HousesPlaceholder() {
  return (
    <section className="placeholder">
      <h2>Evler</h2>
      <p>
        Ev oluşturma, davet kodu ile katılma ve üye yönetimi bu alanda
        görünecek.
      </p>
      <p className="placeholder-hint">
        <code>frontend/src/features/houses/</code> — frontend-houses agent
      </p>
    </section>
  );
}
