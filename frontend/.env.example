# Boş bırak: Vite proxy API isteklerini backend'e yönlendirir (Docker + local dev)
VITE_API_URL=

# Docker Compose içinde Vite proxy hedefi (yalnızca frontend container)
# VITE_API_TARGET=http://backend:3000

# Tarayıcıdan doğrudan backend'e istek atmak istersen:
# VITE_API_URL=http://localhost:3000
